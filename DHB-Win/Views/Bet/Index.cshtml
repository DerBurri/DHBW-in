@model IEnumerable<DHB_Win.Models.Bet>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Wetten</h1>


<p>
    <a asp-action="Create">Neue Wette erstellen</a>
</p>
<table class="table">
    <thead>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ExpPoints)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Reward)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UidFk2Navigation)
        </th>
        <th></th>
    </tr>
    </thead>
    <tbody>
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ExpPoints)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Reward)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UidFk2Navigation.Uid)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.BetId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.BetId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.BetId">Delete</a>
            </td>
        </tr>
    }
    </tbody>
</table>
