@model IEnumerable<DHB_Win.Models.Placement>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BetIdFkNavigation)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.OptionIdFkNavigation)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UidFkNavigation)
        </th>
        <th></th>
    </tr>
    </thead>
    <tbody>
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BetIdFkNavigation.BetId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OptionIdFkNavigation.OptionsId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UidFkNavigation.Id)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new {/* id=item.PrimaryKey */}) |
                @Html.ActionLink("Details", "Details", new {/* id=item.PrimaryKey */}) |
                @Html.ActionLink("Delete", "Delete", new {/* id=item.PrimaryKey */})
            </td>
        </tr>
    }
    </tbody>
</table>
